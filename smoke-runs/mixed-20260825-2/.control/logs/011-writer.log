Candidate 003 is written to `drafts/article-003.md`.

Changes applied from the PM decisions on article-002:

- **clarity-001 (`valid_must_fix`)** — The sentence "Provider, model, and prompt bundle are fixed per role" now separates the two scopes: "Within a role's profile, provider and model are each fixed to a single declared value; the run as a whole is launched from one fixed prompt bundle." The per-role claim no longer includes the bundle, so it reconciles with the earlier run-level definition.
- **clarity-002 (`valid_optional`)** — Applied as well, since it was a small precision fix: "successful, blocked, timed-out, and non-zero invocations" became "invocations that succeeded, blocked, timed out, or exited non-zero," giving "non-zero" an explicit noun.

Left unchanged: story-001 and clarity-003, both ruled `invalid` by the PM — the lead still defines *profile* where it first appears, and the prose role/provider names are not re-annotated with schema identifiers.

The article stays under 700 words, keeps the eight-row table complete, and carries no placeholders.
